jdbc {
  sourcedb {
    tables: [
      {
        name: "employees"
        increment: false
        primarykey: ["emp_no"]
      },
      {
        name: "departments"
        increment: false
        primarykey: ["dept_no"]
      }
    ]
  }
}
